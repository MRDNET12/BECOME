services:
  - type: web
    name: become-pv
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npx prisma generate && npm run build && npx prisma db push
    startCommand: node .next/standalone/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: become-pv-db
          property: connectionString
      - key: NEXTAUTH_SECRET
        generateValue: true
      - key: NEXTAUTH_URL
        value: https://become-pv.onrender.com

databases:
  - name: become-pv-db
    plan: free
    databaseName: becomepv
    user: becomepv
